<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TravelAgencyApp.Views.ProfileView"
             Title="Profile"
             BackgroundColor="White">
    <VerticalStackLayout Margin="20,40,20,40" Padding="10" BackgroundColor="White">
        <Border HeightRequest="200" WidthRequest="200" HorizontalOptions="Center" BackgroundColor="{StaticResource Secondary}" StrokeThickness="2">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="100"/>
            </Border.StrokeShape>
            <Image Source="user.png" Aspect="AspectFill"/>
        </Border>
        <Border BackgroundColor="{StaticResource Secondary}" Margin="30" Padding="20" WidthRequest="350">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="10"/>
            </Border.StrokeShape>
            <HorizontalStackLayout>
                <Label Text="Email: " FontSize="18" TextColor="Black"></Label>
                <Label Text="{Binding}"></Label>
                <Button></Button>
            </HorizontalStackLayout>
        </Border>
    </VerticalStackLayout>

</ContentPage>